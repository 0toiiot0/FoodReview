# DESIGN — 맛집 리뷰 서비스 (가칭)

> PRD.md 기반 / 대상 범위: 랜딩 페이지
> 작성일: 2026-08-14

---

## 1. 무드

**따뜻하고 편안한 — 식탁, 종이 메뉴판.**
정제된 편집형 매거진 레이아웃 위에 올린다.

핵심 긴장: *따뜻함*은 자칫 손글씨·둥근 모서리·수제 감성으로 흐르고, *편집형*은 자칫 차가운 신문 지면이 된다. 이 문서의 결정들은 대부분 **재질은 따뜻하게, 조판은 엄격하게** 라는 원칙에서 나온다.

**하는 것**
- 종이의 질감을 색과 여백으로만 표현
- 활자 크기·굵기 대비로 위계 구성
- 헤어라인 규칙선으로 구획

**하지 않는 것**
- 그림자, 그라데이션, 둥근 카드
- 손글씨체, 일러스트 아이콘
- 음식 사진의 채도 과장

---

## 2. 컬러

저채도 단색 계열. 색은 6개면 충분하고, 그 이상은 이 방향을 망가뜨린다.

| 토큰 | HEX | 용도 |
|------|-----|------|
| `--paper` | `#EFEAE0` | 기본 배경 (오트밀) |
| `--paper-deep` | `#E5DED1` | 교차 섹션 배경, 카드 바탕 |
| `--ink` | `#26221E` | 제목·본문 |
| `--ink-muted` | `#6F675C` | 보조 텍스트, 캡션, 날짜 |
| `--rule` | `#D2C9B9` | 구분선 (1px 헤어라인) |
| `--ink-invert` | `#FBF8F2` | 어두운 면 위 텍스트 |

### 액센트 컬러를 두지 않는 이유

저채도 단색에 강한 포인트 색을 하나 얹으면 그 색이 화면의 주인이 되어 "차분함"이 사라진다. 대신 **명도 대비를 액센트로 쓴다.**

- **CTA 버튼** — `--ink` 솔리드 배경 + `--ink-invert` 텍스트. 페이지에서 유일하게 어두운 덩어리이므로 자연히 시선을 끈다.
- **CTA는 랜딩 전체에 1개.** 대비 전략이라 두 번째 어두운 버튼이 생기는 순간 효과가 반감된다.
- **별점** — 아래 시그니처 항목 참조. 노란 별을 쓰지 않는다.

---

## 3. 타이포그래피

**세리프 제목 + 산세리프 본문.**

| 역할 | 서체 | 비고 |
|------|------|------|
| Display (제목) | **Gowun Batang** (고운바탕) | 명조 중 획 대비가 낮아 따뜻함. 나눔명조보다 덜 흔하다 |
| Display 보조 (라틴/숫자) | **Newsreader** | 편집형 성격의 세리프. 숫자 형태가 좋다 |
| Body (본문·UI) | **Pretendard** | 한글 가독성 기준. 웨이트 400/500/600만 사용 |

전부 웹폰트로 조달 가능하다.

### 타입 스케일

| 이름 | 크기 (desktop / mobile) | 서체 | 비고 |
|------|------|------|------|
| Display | 56 / 36 | Gowun Batang 700 | Hero 헤드라인 전용 |
| H2 | 32 / 24 | Gowun Batang 700 | 섹션 제목 |
| H3 | 20 / 18 | Gowun Batang 400 | 카드 내 식당명 |
| Body | 17 / 16 | Pretendard 400 | 행간 1.75 |
| Small | 14 / 13 | Pretendard 400 | 후기 본문 보조 |
| Label | 12 / 12 | Pretendard 500 | 자간 0.12em, 대문자/짧은 한글 |

### 조판 규칙

- 한글 제목은 자간 `-0.02em`. 명조는 기본 자간이 성기게 보인다
- 본문 최대 폭 `62ch`. 매거진 감각은 행 길이 통제에서 나온다
- 굵기는 400과 700만. 중간값을 섞으면 위계가 흐려진다

---

## 4. 레퍼런스 방향

**데일리 리추얼 매거진 — 정제된 편집형 레이아웃.**

가져올 것:
- 넓은 바깥 여백과 비대칭 그리드
- 섹션을 나누는 얇은 규칙선
- 작은 라벨(eyebrow)로 구획을 표시하는 방식

가져오지 않을 것:
- 신문식 다단 조판 (모바일에서 무너지고, 콘텐츠 양도 부족하다)
- 대형 풀블리드 사진 (V1 데이터 미정 상태에서 리스크)

---

## 5. 시그니처 요소

### 활자로 조판한 별점

별 아이콘 대신, **잡지 리뷰 칼럼처럼 세리프 숫자를 크게 조판한다.**

```
┌──────────────────────────┐
│  4.5   ─────────────     │
│  ↑     을지로 3가 · 한식   │
│  Newsreader 700, 40px    │
└──────────────────────────┘
```

- 평점 숫자 = Newsreader 700, 본문 대비 압도적 크기
- 옆에 `--ink-muted` 로 작은 라벨
- 노란 별을 없애 저채도 팔레트를 지키면서, 동시에 "편집된 지면" 느낌을 만드는 요소

랜딩에서 기억에 남는 건 이 하나면 된다. 나머지는 조용히 간다.

---

## 6. 섹션별 적용

### 6.1 Hero

**목표: 3초 안에 무슨 서비스인지 알게 한다.**

```
┌────────────────────────────────────────┐
│                                        │
│  서비스명 (Display)                      │
│  한 줄 소개                              │
│  ──────────────────────                │
│                                        │
│  ┌──────────────────────┬───────────┐  │
│  │ 어디서 만나세요?        │  찾기 ▸   │  │
│  └──────────────────────┴───────────┘  │
│                                        │
└────────────────────────────────────────┘
```

- 배경 `--paper`, **좌측 정렬**. 편집형은 좌측 정렬에서 나온다
- 뷰포트 100%로 채우지 않는다. 아래 섹션이 살짝 보이게 잘라 스크롤 유도
- **검색창 + 버튼 결합형**을 권장. 시작 버튼만 두는 것보다 서비스 성격이 즉시 전달된다

**검색창 조판**
- 입력 필드: 배경 투명, `--rule` 1px 하단 밑줄만. 박스로 감싸지 않는다
- 버튼: `--ink` 솔리드 + `--ink-invert`. 페이지에서 유일한 어두운 덩어리(푸터 제외)
- placeholder는 `--ink-muted`, Pretendard 400. "검색어를 입력하세요" 같은 시스템 말투 대신 사용자 상황을 부르는 문장

### 6.2 인기 맛집 카드

가짜 데이터 3~6개. 나중에 실데이터로 교체된다.

```
┌─────────────┬─────────────┬─────────────┐
│ [ 썸네일 ]   │ [ 썸네일 ]   │ [ 썸네일 ]   │
│             │             │             │
│ 4.6         │ 4.4         │ 4.2         │
│ 식당명       │ 식당명       │ 식당명       │
│ 지역 · 분류   │ 지역 · 분류   │ 지역 · 분류   │
└─────────────┴─────────────┴─────────────┘
```

- **그림자·둥근 모서리 없음.** 카드는 `--paper-deep` 면 또는 `--rule` 헤어라인 테두리로만 구분
- 평점은 시그니처 조판 적용 — Newsreader 700, 32px
- 썸네일 비율 4:3 고정. 이미지가 없어도 `--paper-deep` 플레이스홀더로 레이아웃 유지
- 데스크톱 3열 / 태블릿 2열 / 모바일 1열
- 호버: 배경만 한 단계 어둡게. 들어올리거나 확대하지 않는다

> 가짜 데이터라도 **실제 있을 법한 식당명과 지역**을 쓴다. "맛집 A, 맛집 B"는 미완성으로 보이고, 편집형 레이아웃일수록 더 티가 난다.

### 6.3 최근 리뷰 (흐르는 띠)

살아있는 서비스처럼 보이게 하는 장치.

```
──────────────────────────────────────────
 4.5 “회식하기 딱 좋았어요” · 을지로  4.0 “재…
──────────────────────────────────────────
        ← 좌측으로 천천히 흐름
```

- 위아래 `--rule` 헤어라인으로 띠 영역을 명확히 구획
- 한 줄 구성: 평점(Newsreader) + 리뷰 한 줄(Pretendard) + 지역(`--ink-muted`)
- 항목 사이 구분자는 `·` 또는 짧은 세로선

**모션 규칙 (7장의 예외)**
- 좌측 방향 무한 루프, **60초 이상**의 느린 속도. 빠르면 읽을 수 없고 페이지 톤도 깨진다
- 호버 시 정지
- `prefers-reduced-motion` 시 **완전 정지**, 가로 스크롤 가능한 정적 목록으로 전환
- 양 끝은 `--paper` 마스크로 페이드 아웃 (그라데이션 금지 원칙의 유일한 예외 — 장식이 아니라 잘림 처리이므로 허용)

### 6.4 서비스 특징

앞으로 생길 기능(AI 요약 · 감성 분석 · 대시보드)을 미리 소개.

```
┌────────────────────────────────────────┐
│ AI 리뷰 요약            [COMING SOON]   │
│ 한두 줄 설명                             │
│ ────────────────────────────────────── │
│ 감성 분석              [COMING SOON]   │
│ ...                                    │
└────────────────────────────────────────┘
```

- 카드 그리드가 아니라 **헤어라인으로 나눈 목록**. 6.2에서 이미 카드를 썼으므로 반복하지 않는다
- 아이콘 없음

**COMING SOON 뱃지 규격**
- 영문 대문자 고정. 한글 "준비 중"과 섞으면 명조·산세리프에 서체가 하나 더 늘어 조판이 흐트러진다
- Pretendard 500 / 11px / 자간 `0.14em`
- 배경 없음. `--rule` 1px 테두리 + `--ink-muted` 텍스트, radius 0
- **어둡게 칠하지 않는다.** 미구현 기능이 CTA보다 눈에 띄면 안 된다

### 6.5 푸터

`--ink` 배경 + `--ink-invert` 텍스트. 페이지를 어두운 면으로 닫는다.

- 만든 사람
- GitHub 링크
- 프로젝트 허브(MY HUB)로 돌아가는 링크
- 링크는 밑줄 없이, 호버 시에만 밑줄

※ Hero 버튼과 함께 어두운 덩어리가 2개다. 푸터가 페이지 최하단이라 동시에 보이는 경우는 드물지만, 구현 후 확인 필요.

---

## 7. 모션

- 스크롤 진입 시 `opacity 0→1` + `translateY 12px→0`, 400ms
- 호버는 색 변화만. 이동·확대·그림자 없음
- **예외 하나:** 6.3 최근 리뷰 띠의 무한 흐름. 이 페이지에서 유일하게 지속되는 움직임이므로, 다른 곳에 움직임을 추가하면 이 장치가 죽는다
- `prefers-reduced-motion` 존중 (티커 포함 전부 정지)

---

## 8. 미결정 사항

- [ ] 별점 표기 척도 — 5점 소수점(4.5) 확정 여부. PRD의 별점 척도 결정과 연동
- [ ] Hero — 검색창 결합형 / 시작 버튼 단독 중 최종 선택
- [ ] 가짜 데이터 식당명·리뷰 문구 작성 (실제로 있을 법하게)
- [ ] 인기 맛집 카드 썸네일 이미지 조달 방법
- [ ] 티커 정지 조건 — 호버 정지 외에 클릭 시 상세로 이동시킬지
- [ ] Gowun Batang 실 렌더링 확인 — 작은 크기에서 획이 뭉치는지
