# DESIGN — 어디갈래

> PRD.md 기반 / 대상 범위: 랜딩 페이지 + 맛집 검색 페이지(restaurants.html)
> 작성일: 2026-08-14
> **갱신: 2026-08-20 — 에어비앤비 디자인 시스템으로 전면 교체 (v2)**

---

## 0. v2 갱신 노트

이전 버전(정제된 편집형 매거진 디자인)은 사용자가 명시적으로 폐기를 확정했다. 그림자·그라데이션·둥근 모서리 금지, 무채색 대비만 사용, CTA 페이지 전체 1개 같은 제약은 **더 이상 유효하지 않다.** 아래 내용이 현재 기준이다. 이전 버전의 "하지 않는 것" 목록은 이번 버전에서 전부 뒤집혔다고 보면 된다.

---

## 1. 무드

**따뜻하고 신뢰감 있는 — 에어비앤비 스타일의 부드러운 카드형 UI.**

핵심 원칙: **재질은 부드럽게, 포인트는 선명하게.** 넓은 여백과 둥근 모서리, 은은한 그림자로 따뜻함을 만들고, 코럴 액센트 컬러로 행동 유도 지점을 명확히 찍는다.

**하는 것**
- 카드에 radius + shadow로 입체감을 준다
- hover 시 카드가 살짝 떠오르는 피드백 (translateY + shadow 강화)
- 코럴/레드(`#FF385C` 계열) 액센트를 CTA·배지·별점 아이콘 등 포인트에 반복 사용
- pill 형태 버튼/배지

**하지 않는 것**
- 무채색 대비만으로 위계를 만드는 것 (이제 색으로도 위계를 만든다)
- 세리프 활자로 조판한 별점 (에어비앤비 톤과 안 맞음 — 3장 참조)

---

## 2. 컬러

| 토큰 | HEX | 용도 |
|------|-----|------|
| `--paper` | `#FFFFFF` | 기본 배경 |
| `--paper-deep` | `#F7F7F7` | 교차 섹션 배경, 푸터, 카드 플레이스홀더 |
| `--ink` | `#222222` | 제목·본문 |
| `--ink-muted` | `#717171` | 보조 텍스트, 캡션, 메타 정보 |
| `--rule` | `#EBEBEB` | 옅은 구분선, 카드 테두리 보조 |
| `--ink-invert` | `#FFFFFF` | 어두운/액센트 면 위 텍스트 |
| `--accent` | `#FF385C` | 코럴 포인트 컬러 — CTA, 별점 아이콘, 활성 상태 |
| `--accent-deep` | `#E31C5F` | 액센트 hover/active |
| `--accent-soft` | `#FFE8EC` | 배지·태그 배경 (연한 코럴) |

### 액센트를 다시 도입한 이유

이전 버전은 "색이 주인이 되면 차분함이 사라진다"는 이유로 액센트를 배제했다. 이번 방향은 정반대 전제에서 출발한다 — **에어비앤비 시스템은 코럴 하나를 반복 노출해 신뢰·행동 유도 지점을 즉시 인지시키는 것이 핵심이다.** 액센트는 이제 다음 지점에 자유롭게 쓴다: CTA 버튼(페이지 내 여러 개 허용), 별점 아이콘, COMING SOON류 배지, 활성 필터/카테고리, 링크 hover.

**단, 톤은 통일한다** — radius, shadow 강도, 액센트 컬러 값은 페이지 전체(랜딩 + `restaurants.html`)에서 동일하게 유지해 일관성을 지킨다.

---

## 3. 타이포그래피

**세리프를 버리고 Pretendard 단일 서체로 간다.**

| 역할 | 서체 | 비고 |
|------|------|------|
| Display/H2/H3 (제목) | **Pretendard** 700 | |
| Body/UI | **Pretendard** 400 / 500 / 600 | |

### 세리프(Gowun Batang / Newsreader)를 뺀 이유

이전 버전의 시그니처는 "세리프 숫자로 조판한 별점"이었다. 에어비앤비 산세리프 톤에서는 이 장치가 이질적이다. 두 가지 선택지를 검토했다:

1. 세리프 제목만 유지하고 나머지를 산세리프로 바꾼다 (믹스 유지)
2. 세리프를 완전히 버리고 Pretendard 하나로 통일한다

**2번을 택했다.** 이유:
- 에어비앤비 정체성은 "따뜻함을 서체 대비가 아니라 형태(radius)·색(코럴)·그림자로 만든다"는 데 있다. 세리프 제목을 남기면 두 정체성이 한 페이지에서 충돌한다.
- 웹폰트 두 벌(Gowun Batang, Newsreader)을 더 이상 로드하지 않아 페이지가 가벼워진다.
- Pretendard는 400~700 전 웨이트에서 한글·라틴·숫자 가독성이 이미 충분해, 굳이 별도 세리프로 숫자 조판을 강조할 필요가 없다.

별점은 이제 **Pretendard 700 굵은 숫자 + 코럴 별 아이콘**(기존 마크업에 있던 미사용 `#star-shape` SVG 심볼을 재활용) 조합으로 표기한다. 에어비앤비 리뷰 UI(★ 4.86 형태)와 동일한 관습이다.

### 타입 스케일

| 이름 | 크기 (desktop / mobile) | 웨이트 | 비고 |
|------|------|------|------|
| Display | 48 / 32 | 700 | Hero 헤드라인 |
| H2 | 28 / 22 | 700 | 섹션 제목 |
| H3 | 18 / 17 | 700 | 카드 내 식당명 |
| Body | 16 / 15 | 400 | 행간 1.6 |
| Small | 14 / 13 | 400/500 | 메타, 캡션 |
| Label | 12 / 12 | 600 | 자간 0.04em, 배지·eyebrow |

### 조판 규칙

- 자간은 거의 0에 가깝게 (`-0.01em` 제목, 배지만 약간의 자간 부여)
- 본문 최대 폭 60ch 내외 유지 (가독성은 여전히 중요)
- 웨이트 400/500/600/700 자유롭게 사용 (이전의 "400·700만" 제약 폐기)

---

## 4. 레퍼런스 방향

**에어비앤비 검색 결과/랜딩 페이지.**

가져올 것:
- 넉넉한 화이트스페이스 + 카드 그리드
- 카드: radius 12px, 은은한 shadow, hover 시 상승 피드백
- pill 버튼/배지, 코럴 액센트의 반복 노출
- 검색바를 둥글고 큰 결합형 컴포넌트로 (Hero + restaurants.html 공통)

가져오지 않을 것:
- 대형 히어로 풀블리드 사진 (V1 데이터 미정 상태 리스크는 여전함 — 대신 은은한 코럴 톤 배경 블러/블롭으로 따뜻함 표현)
- 과도한 그림자/네온 톤 (그림자는 절제된 확산광 수준으로 — `0 2px 8px rgba(0,0,0,.08)` ~ `0 10px 30px rgba(0,0,0,.16)` 범위 내)

---

## 5. 시그니처 요소

### 별 아이콘 + 굵은 숫자 별점

```
┌──────────────────────────┐
│  ★ 4.5    을지로 3가 · 한식 │
│  코럴 별, Pretendard 700  │
└──────────────────────────┘
```

- 별 아이콘: 기존 마크업의 `#star-shape` SVG 심볼을 실제로 사용 (이전 버전은 미사용 상태로 방치했었음). `fill: var(--accent)`
- 숫자: Pretendard 700, 카드에서 20px 내외
- 에어비앤비의 "★ 4.86 (128)" 관습을 참고하되, 후기 개수는 V1 가짜 데이터 범위상 생략 가능

---

## 6. 섹션별 적용

### 6.1 Hero

- 배경 `--paper` + 은은한 코럴 blur 블롭 (장식, 저채도 `--accent-soft` 계열 radial-gradient, 텍스트 가독성 방해 없는 강도)
- 중앙 정렬 또는 좌측 정렬 모두 허용되나, 검색 컴포넌트는 **크고 둥근 pill 형태의 결합형 바**로 — input과 버튼이 하나의 radius-pill 컨테이너 안에서 시각적으로 이어지도록
- CTA 버튼: `--accent` 배경, 흰 텍스트, radius-pill, `--shadow-md`, hover 시 `--accent-deep` + 살짝 상승

### 6.2 인기 맛집 카드

- 카드: `--paper` 배경, radius 12px, `--shadow-sm` 기본 → hover 시 `--shadow-md` + `translateY(-4px)`
- 썸네일: 카드 상단에 radius 매칭 (카드와 동일한 top radius), 4:3 비율 플레이스홀더 유지
- 평점: 5장 시그니처 요소(별 아이콘 + 굵은 숫자)
- 데스크톱 3열 / 태블릿 2열 / 모바일 1열 (유지)
- 섹션 헤더에 `restaurants.html`(맛집 검색 페이지)로 이동하는 pill 아웃라인 버튼 "맛집 더 보기" 추가 — 실제 동작하는 내비게이션 링크 (가짜 기능 아님, 페이지 간 이동)

### 6.3 최근 리뷰 (흐르는 띠)

- 항목을 rounded pill/카드 칩 형태로 (기존 세로선 구분자 대신 각 항목을 은은한 shadow가 있는 개별 칩으로)
- **모션 규칙은 유지한다** (아래 7장 참조) — `script.js`의 `-50%` 루프 로직은 그대로, 시각 스타일만 카드형으로 교체

### 6.4 서비스 특징

- 헤어라인 목록 대신 **카드/그리드 형태**로 전환 가능 (에어비앤비는 카드 반복을 꺼리지 않음). hover 시 은은한 shadow 상승
- COMING SOON 배지: `--accent-soft` 배경 + `--accent-deep` 텍스트, radius-pill

### 6.5 푸터

- **다크 블록에서 라이트 그레이(`--paper-deep`) 블록으로 변경.** 에어비앤비 푸터는 어두운 배경을 쓰지 않고, 옅은 회색 배경 + 다크 텍스트 + 상단 헤어라인으로 마감한다. 이전 버전의 "페이지를 어두운 면으로 닫는다" 컨셉은 폐기.
- 만든 사람 / GitHub / MY HUB 링크 구성은 유지

### 6.6 restaurants.html (신규, logic 팀원 소유 — 여기 CSS만 어디갈래 팀이 작성)

- 검색바: Hero와 동일한 톤의 pill 결합형 (input + select + button)
- 카테고리 필터: pill 토글 (선택 시 `--accent` 배경)
- 결과 카드 그리드: 6.2 인기 맛집 카드와 동일한 radius/shadow 톤 (텍스트 정보는 카카오 로컬 API 응답 필드 — 이름/카테고리/주소/전화 기준으로 대응)

---

## 7. 모션

- 스크롤 진입 시 `opacity 0→1` + `translateY 12px→0`, 400ms (유지)
- 카드 hover: `translateY(-4px)` + shadow 강화 + `transform/box-shadow` 트랜지션 200ms (신규 허용)
- 버튼 hover: 배경색 변화 + 미세 상승 허용
- **최근 리뷰 띠의 무한 흐름은 페이지에서 가장 눈에 띄는 지속 모션이라는 개념 자체는 유지** — 60~90초 저속 루프, hover 정지, `prefers-reduced-motion` 시 완전 정지. 다만 카드 hover 등 짧은 트랜지션은 이제 다른 곳에도 허용된다 (이전처럼 "티커 외 모션 금지"는 아님 — 단, 지속적/반복적 루프 애니메이션은 티커 외에 추가하지 않는다)

---

## 8. 미결정 사항

- [ ] 별점 표기 척도 — 5점 소수점(4.5) 확정 여부
- [x] `restaurants.js`가 렌더링하는 카드 마크업 확인 완료 — `article.result-card` + `.result-card__name` / `__category` / `__address` / `__phone` / `__link`. 카카오 로컬 API 응답에 이미지 필드가 안 붙어 있어 썸네일 없이 텍스트 카드로 스타일링함
- [ ] 가짜 데이터 식당명·리뷰 문구는 기존 것 유지 (실제로 있을 법한 서울 동네/음식 카테고리)
- [ ] 인기 맛집 카드 썸네일 이미지 조달 방법
- [ ] Pretendard 단일 서체 결정에 대한 최종 합의 (현재는 이 문서의 판단으로 확정)
