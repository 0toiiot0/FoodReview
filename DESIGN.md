# DESIGN — 어디갈래

> PRD.md 기반 / 대상 범위: 랜딩 페이지 + 맛집 검색 페이지(restaurants.html) + 마이페이지(mypage.html)
> 작성일: 2026-08-14
> 갱신: 2026-08-20 — 에어비앤비 디자인 시스템으로 전면 교체 (v2)
> 갱신: 2026-08-24 — 스포티파이 스타일 다크 테마로 전면 교체 (v3)
> **갱신: 2026-08-24 — 닌텐도(Nintendo.com) 스타일로 전면 교체 (v4)**

---

## 0. v4 갱신 노트

v3(스포티파이 스타일 — 다크 배경, 그린 액센트, 배경색 단계 elevation)는 사용자가 명시적으로 폐기를 확정했다. **지금 기준은 닌텐도(Nintendo.com) 웹사이트 스타일이다.** 방향이 여러 갈래일 수 있어 사용자에게 확인했고, "Nintendo.com 웹사이트 스타일"(레트일 사이트 톤 — 흰 배경 + 레드 액센트 + 딱딱한 오프셋 그림자)을 선택했다. 핵심 전환:

- 배경이 **다크 → 라이트**로 되돌아간다 (`#121212` → `#FFFFFF`). v2의 화이트 배경과 방향은 같지만 액센트·그림자 스타일은 다르다 — 단순 롤백이 아니다.
- 액센트가 **그린(`#1DB954`) → 닌텐도 레드(`#E60012`)**로 바뀐다.
- 액센트 위 텍스트는 다시 **흰색**(`--ink-invert:#FFFFFF`)으로 돌아간다 — 스포티파이의 "검정 텍스트" 관습은 닌텐도에 안 맞는다 (닌텐도 CTA는 레드 배경 + 흰 글씨).
- **elevation을 표현하던 배경색 단계 방식은 유지하되, shadow의 성격이 완전히 바뀐다.** 부드럽게 번지는 blur shadow가 아니라 **블러 없는 딱딱한 오프셋 그림자**(`3px 3px 0 rgba(0,0,0,.9)` 식)를 쓴다 — 게임 콘솔 버튼처럼 카드/버튼이 눌리고 떠오르는 듯한 촉각적 느낌을 준다. hover 시 그림자가 더 커지고(`5px→7px`) 표면이 `--paper-deep`(옅은 회색)에서 `--surface-hover`(흰색)로 밝아지며 함께 살짝 떠오른다(`translateY`).
- radius는 v3의 8px/6px보다 다시 조금 키운 14px(카드)/8px(인풋) — 닌텐도 UI는 아이콘 타일이나 버튼에서 친근하고 둥근 모서리를 쓴다. pill(999px) 버튼은 유지.

이전 버전(v1 매거진, v2 에어비앤비, v3 스포티파이)의 세부 규칙은 이 문서 하단 "히스토리" 절에 요약만 남기고, 아래 1~8장은 v4 기준으로 다시 쓴다.

---

## 1. 무드

**밝고 경쾌한, 촉각적인 — 닌텐도 리테일 사이트 스타일.**

핵심 원칙: **배경은 밝게, 포인트는 레드 하나로, 인터랙션은 눌리고 떠오르는 느낌으로.** 페이지 전체를 흰색으로 깔고, 카드/버튼은 블러 없는 딱딱한 오프셋 그림자로 "종이 위에 얹힌 카드"가 아니라 "눌렀다 뗄 수 있는 버튼"처럼 보이게 한다.

**하는 것**
- 페이지 배경 흰색(`#FFFFFF`), 카드/모달 등 표면은 옅은 회색(`#F4F4F4`) → hover 시 흰색(`#FFFFFF`)으로 다시 밝아지며 그림자가 커진다
- 닌텐도 레드(`#E60012` 계열) 액센트를 CTA·별점 아이콘·활성 필터·배지 등 포인트에 반복 사용
- 액센트 배경 위 텍스트는 **흰색**
- **블러 없는 딱딱한 오프셋 그림자**(hard shadow)를 카드·버튼에 기본 적용 — 흐릿하게 번지는 그림자 금지
- pill 형태 버튼/배지 유지, 카드 radius는 14px로 친근하게

**하지 않는 것**
- 다크 배경 (v3의 블랙/그린 조합은 완전히 폐기)
- 부드럽게 blur된 그림자 (`box-shadow`에 blur 반경을 주는 것) — 닌텐도 톤은 딱딱한 오프셋이어야 한다
- 액센트 면 위에 검정 텍스트를 쓰는 것 (v3의 "검정 텍스트" 관습은 폐기, 다시 흰 텍스트로)

---

## 2. 컬러

| 토큰 | HEX / 값 | 용도 |
|------|-----|------|
| `--paper` | `#FFFFFF` | 페이지 기본 배경 |
| `--paper-deep` | `#F4F4F4` | 카드, 모달, 헤더/푸터 등 표면의 기본(휴지) 상태 |
| `--surface-hover` | `#FFFFFF` | 카드/티커 칩 hover 시 표면 — 다시 순백으로 밝아지며 그림자가 커짐(=떠오름) |
| `--ink` | `#1A1A1A` | 제목·본문 (완전한 블랙 대신 살짝 부드러운 다크) |
| `--ink-muted` | `#6E6E6E` | 보조 텍스트, 캡션, 메타 정보 |
| `--rule` | `#E4E4E4` | 옅은 구분선, 카드 테두리 보조 |
| `--ink-invert` | `#FFFFFF` | 액센트(레드) 면 위 텍스트 — 흰색 |
| `--accent` | `#E60012` | 닌텐도 레드 — CTA, 별점 아이콘, 활성 상태 |
| `--accent-deep` | `#B8000E` | 액센트 hover/active (더 어두운 레드 — 눌린 느낌) |
| `--accent-soft` | `rgba(230,0,18,.12)` | 배지·태그 배경 (옅은 레드 틴트) |
| `--accent-wash` | `rgba(230,0,18,.06)` | 장식용 블러 블롭 배경 (더 옅은 레드 틴트) |

### 액센트를 레드로 바꾼 이유

닌텐도의 브랜드 컬러는 레드(`#E60012`) 하나로 통일돼 있고, 웹사이트도 이 레드를 CTA·강조 지점에 반복 노출하는 방식을 쓴다. 그린(스포티파이)이나 코럴(에어비앤비)과 달리 **레드 위에는 흰 텍스트**를 쓰는 게 관습이라, `--ink-invert`를 다시 흰색으로 되돌렸다 — 이 토큰의 값이 벌써 세 번째 바뀌는 것이므로, 새 액센트 버튼/배지를 추가할 때는 항상 "이 액센트 배경 위에 어떤 색 텍스트가 관습적으로 맞는지"부터 확인한다.

**단, radius·shadow 스타일·액센트 컬러 값은 페이지 전체(index.html + restaurants.html + mypage.html)에서 동일하게 유지**해 일관성을 지킨다 — 세 CSS 파일이 각자 동일한 `:root` 토큰 블록을 중복 정의하고 있으므로, 토큰을 바꿀 때는 항상 세 파일 모두 동기화한다.

---

## 3. 타이포그래피

**Pretendard 단일 서체 유지.** (v2에서 세리프를 버린 이후 이 결정은 테마가 몇 번 바뀌어도 그대로 유지된다.)

| 역할 | 서체 | 비고 |
|------|------|------|
| Display/H2/H3 (제목) | **Pretendard** 700 | |
| Body/UI | **Pretendard** 400 / 500 / 600 | |

### 타입 스케일 (변경 없음)

| 이름 | 크기 (desktop / mobile) | 웨이트 | 비고 |
|------|------|------|------|
| Display | 48 / 32 | 700 | Hero 헤드라인 |
| H2 | 28 / 22 | 700 | 섹션 제목 |
| H3 | 18 / 17 | 700 | 카드 내 식당명 |
| Body | 16 / 15 | 400 | 행간 1.6 |
| Small | 14 / 13 | 400/500 | 메타, 캡션 |
| Label | 12 / 12 | 600 | 자간 0.04em, 배지·eyebrow |

별점은 계속 **Pretendard 700 굵은 숫자 + 레드 별 아이콘**(`#star-shape` SVG, `fill:var(--accent)`) 조합. 색만 그린 → 레드로 바뀌었을 뿐, 조판 관습 자체는 이전 버전과 동일하다.

---

## 4. 레퍼런스 방향

**Nintendo.com 리테일 웹사이트.**

가져올 것:
- 밝고 깨끗한 흰 배경 + 레드 액센트의 반복 노출
- **블러 없는 딱딱한 오프셋 그림자** — 카드/버튼이 눌리고 떠오르는 듯한 촉각적 인터랙션 (게임 콘솔 버튼을 연상시키는 느낌)
- pill/둥근 사각형 버튼, 친근한 radius(14px)
- hover 시 표면이 순백으로 밝아지며 그림자가 커지는(=떠오르는) 인터랙션

가져오지 않을 것:
- 다크 배경, 그린/코럴 계열 색상 (v2·v3 완전 폐기)
- 부드럽게 blur되는 그림자 — 닌텐도 톤에서는 그림자에 흐림이 없어야 한다
- Switch 콘솔 홈 화면 UI나 사이드바 같은 앱 셸 구조 — 이 프로젝트는 여전히 콘텐츠 중심 랜딩/리스트 페이지이므로 컬러·표면·그림자 언어만 가져오고 앱 UI 구조는 가져오지 않는다

---

## 5. 시그니처 요소

### 별 아이콘 + 굵은 숫자 별점

```
┌──────────────────────────┐
│  ★ 4.5    을지로 3가 · 한식 │
│  레드 별, Pretendard 700  │
└──────────────────────────┘
```

- 별 아이콘: `#star-shape` SVG 심볼, `fill:var(--accent)` (레드)
- 숫자: Pretendard 700, 카드에서 20px 내외

### 딱딱한 오프셋 그림자 (hard shadow)

```
default:  box-shadow: 3px 3px 0 rgba(0,0,0,.9);
hover:    box-shadow: 7px 7px 0 rgba(0,0,0,.9);  + translateY(-4px)
```

blur 반경을 주지 않고 그림자를 그대로 오프셋만 준다 — 카드/버튼이 종이에 눌린 도장처럼 보이다가, hover 시 그림자가 커지며 살짝 떠오르는 효과를 낸다. 이 프로젝트 전체에서 **모든 shadow 토큰(`--shadow-sm/md/lg`)이 이 규칙을 따른다** — 새 컴포넌트에 그림자를 추가할 때 blur 값을 넣지 않도록 주의한다.

### 레드 버튼 + 흰 텍스트

CTA·로그인 버튼·활성 필터 칩 등 액센트 배경 위에는 항상 `color:var(--ink-invert)`(흰색)를 쓴다.

---

## 6. 섹션별 적용

### 6.1 Hero

- 배경 `--paper`(흰색) + 은은한 레드 blur 블롭 (장식, `--accent-soft`/`--accent-wash` radial-gradient)
- 검색 컴포넌트: 크고 둥근 pill 형태의 결합형 바 — 배경은 `--paper`, 테두리 `--rule`
- CTA 버튼: `--accent`(레드) 배경, **흰 텍스트**, radius-pill, hard shadow, hover 시 `--accent-deep`(더 어두운 레드) + 그림자 확대

### 6.2 인기 맛집 카드

- 카드: `--paper-deep`(옅은 회색) 배경 → hover 시 `--surface-hover`(흰색)로 밝아지며 `--shadow-lg`(hard shadow) + `translateY(-4px)`
- 썸네일 플레이스홀더: 다크 뉴트럴(`#242424`) → 라이트 뉴트럴(`#F0EDE9`, hover `#E9E4DC`)로 다시 교체
- 평점: 5장 시그니처 요소(레드 별 + 굵은 숫자)
- 데스크톱 3열 / 태블릿 2열 / 모바일 1열 (유지)

### 6.3 최근 리뷰 (흐르는 띠)

- 티커 섹션 배경은 `--paper-deep`, 개별 칩은 `--surface-hover`(흰색)를 써서 칩이 섹션 위에 떠 있는 위계를 유지 (v3에서 다크용으로 뒤집었던 순서가 라이트 테마로 돌아오면서 다시 "칩이 섹션보다 밝다"는 자연스러운 방향이 됐다)
- **모션 규칙은 유지한다** (7장 참조) — `script.js`의 `-50%` 루프 로직은 그대로

### 6.4 서비스 특징

- 카드/그리드 형태 유지, 배경 `--paper-deep` → hover 시 `--surface-hover` + hard shadow
- NOW LIVE 배지: `--accent-soft` 배경 + `--accent-deep` 텍스트, radius-pill (톤만 레드로 교체)

### 6.5 푸터

- `--paper-deep`(옅은 회색) 블록 유지 — v2/v3에서 이미 확정된 "라이트 그레이 푸터" 컨셉을 그대로 가져온다

### 6.6 restaurants.html / mypage.html

- 검색바·필터·저장 버튼 등 모든 컴포넌트가 index.html과 동일한 토큰 세트를 그대로 재사용 (`restaurants.css`/`mypage.css`가 각자 `:root`를 중복 정의하므로 토큰 변경 시 세 파일 모두 동기화 필요)
- 저장/삭제 버튼의 반투명 원형 배경: 다크 배경 가정이던 `rgba(0,0,0,.6)`을 다시 라이트 배경에 맞는 `rgba(255,255,255,.9)`로 교체 (아이콘 색은 `--ink`, 원형 버튼 자체에도 hard shadow 적용)
- 마이페이지의 활성 필터 칩 안 카운트 배지(`.bookmark-filter__count`)는 `--ink-invert`가 검정→흰색으로 바뀌면서 기존 `rgba(255,255,255,.3)` 반투명 배경 + 흰 텍스트 조합이 무효(흰 위에 흰 글씨)가 됐던 걸 발견해 `background:var(--paper)` + `color:var(--accent-deep)`로 교체함 — 액센트 배경 위 텍스트 색(`--ink-invert`)을 바꿀 때는 그 위에 얹힌 2차 배지 색까지 같이 확인해야 한다
- AI 리뷰 분석(감성 바, 워드클라우드)의 긍정/중립/부정 색상(`--positive`/`--neutral`/`--negative`)은 라이트 배경에서도 충분히 대비되는 톤이라 값 유지

---

## 7. 모션

- 스크롤 진입 시 `opacity 0→1` + `translateY 12px→0`, 400ms (유지)
- 카드 hover: 표면이 흰색으로 밝아짐 + hard shadow가 커짐(3px→7px 오프셋) + `translateY(-4px)`, 200ms
- 버튼 hover: 배경색 변화(레드 → 더 어두운 레드) + hard shadow 확대 + 미세 상승
- **최근 리뷰 띠의 무한 흐름은 페이지에서 가장 눈에 띄는 지속 모션이라는 개념 자체는 유지** — 60~90초 저속 루프, hover 정지, `prefers-reduced-motion` 시 완전 정지. 지속적/반복적 루프 애니메이션은 티커 외에 추가하지 않는다

---

## 8. 미결정 사항

- [ ] 별점 표기 척도 — 5점 소수점(4.5) 확정 여부
- [ ] 가짜 데이터 식당명·리뷰 문구는 기존 것 유지 (실제로 있을 법한 서울 동네/음식 카테고리)
- [ ] 인기 맛집 카드 썸네일 이미지 조달 방법 (현재는 라이트 뉴트럴 플레이스홀더)
- [ ] 카드/버튼에 얇은 검정 테두리(outline)까지 추가해 더 "만화/게임 UI" 톤을 강조할지 여부 — v4 1차 적용에서는 hard shadow까지만 반영, 테두리는 범위 밖으로 미룸

---

## 히스토리 (v1 → v2 → v3 → v4 요약)

- **v1 (2026-08-14 최초 작성)**: 정제된 편집형 매거진 스타일. 그림자·그라데이션·둥근 모서리 금지, 무채색 대비만 사용, 세리프(Gowun Batang/Newsreader) 숫자 조판, CTA 페이지 전체 1개.
- **v2 (2026-08-20)**: 에어비앤비 스타일로 전면 교체. 화이트 배경, radius+shadow(부드러운 blur)로 입체감, 코럴(`#FF385C`) 액센트 반복 노출, Pretendard 단일 서체로 세리프 폐기, 여러 개의 CTA 허용.
- **v3 (2026-08-24)**: 스포티파이 스타일 다크 테마로 전면 교체. `#121212` 배경, 배경색 단계 기반 elevation, 그린(`#1DB954`) 액센트 + 액센트 위 검정 텍스트, radius 절제(8px/6px).
- **v4 (2026-08-24, 현재)**: 닌텐도(Nintendo.com) 스타일로 전면 교체. 다시 화이트 배경(`#FFFFFF`)으로 복귀하되 v2와 달리 레드(`#E60012`) 액센트 + **블러 없는 딱딱한 오프셋 그림자**(hard shadow)를 채택, 액센트 위 텍스트는 다시 흰색, radius는 14px로 재조정.
