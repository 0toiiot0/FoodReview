# PRD — 어디갈래

> 작성일: 2026-08-14
> 상태: 랜딩 페이지 단계

---

## 1. 한 줄 정의

**약속을 잡는 사람**이, **모임 전에**, **여럿이 만족할 만한 자리를 정하기 위해** 쓰는 맛집 후기·평점 서비스.

### 이 사용자의 문제

혼자 먹을 곳을 고르는 것과 여럿이 갈 곳을 고르는 것은 판단 기준이 다르다. 총대를 멘 사람은 맛뿐 아니라 **자리·소음·가격대·예약 가능 여부**까지 감당해야 하고, 실패하면 그 책임이 자기에게 온다. 기존 리뷰 서비스는 별점 평균만 보여줄 뿐, "우리 일행이 가도 괜찮은 곳인가"에는 답하지 않는다.

### 포지셔닝 메모

이 서비스의 차별점은 **쌓인 후기를 AI가 요약·분석해 보여주는 것**(로드맵 참조)에 있다. 후기 30개를 다 읽을 시간이 없는 약속 주최자에게, 요약은 단순 편의가 아니라 핵심 기능이 된다.

이 기능은 V1 범위 밖이지만, **랜딩의 "서비스 특징" 섹션에서 COMING SOON으로 명시해 노출한다.** 감추면 이 서비스가 왜 다른지 설명할 방법이 없다.

---

## 2. 오늘의 작업 범위

**랜딩 페이지 1장.** 그 외 기능은 구현하지 않는다.

- 실제 서비스 기능(검색, 후기 작성, 로그인)은 동작하지 않는다
- 인기 맛집 카드와 최근 리뷰는 **가짜 데이터**로 채운다
- Hero 검색창은 배치하되 동작하지 않는다 (비활성 또는 입력만 가능)

---

## 3. 핵심 기능 (제품 완성 기준 / V1)

랜딩 이후 구현할, 서비스가 성립하기 위한 최소 기능 4가지.

| # | 기능 | 설명 |
|---|------|------|
| 1 | 식당 검색 / 목록 | 이름·지역 기준으로 식당을 찾고 목록으로 확인 |
| 2 | 식당 상세 페이지 | 식당 기본 정보 + 해당 식당의 후기 모아보기 |
| 3 | 리뷰 리스트 | 식당과 무관하게 전체 후기를 최신순으로 훑는 화면 |
| 4 | 후기 작성 | 별점 + 텍스트 + 사진 업로드 |
| 5 | 회원가입 / 로그인 | 후기 작성 주체 식별, 내가 쓴 후기 관리 |
| 6 | 데이터 저장 | 후기·계정의 영속 저장. 5번의 전제 조건 |

**2번과 3번의 차이:** 상세 페이지의 후기 목록은 "이 식당 어때?"에 답하고, 리뷰 리스트는 "요즘 사람들 어디 갔나"에 답한다. 목적이 다르므로 별개 화면으로 둔다.

**핵심 사용 흐름:** 검색 → 상세 확인 → 방문 → 후기 작성

---

## 4. 로드맵 (V1 이후)

구현 난이도와 선행 조건 순으로 3단계.

### 1단계 — 필터

목록에서 원하는 조건만 추려내는 기능.

- 지역, 가격대, 카테고리 등으로 결과 좁히기
- **데이터 소스는 정적 JSON** — 별도 백엔드 없이 프론트에서 읽어 필터링
- 대상이 모임 주최자이므로, 인원 수용·예약 가능 같은 조건이 붙으면 가치가 커진다 (미결정 사항 참조)

> ⚠️ **주의:** V1의 "데이터 저장"은 DB를, 이 단계의 필터는 정적 JSON을 전제한다. 두 데이터 소스가 공존하면 후기는 DB에 있는데 식당 목록은 JSON에 있는 상태가 된다. 어느 쪽을 먼저 만들지, 최종적으로 무엇에 통합할지 정해야 한다.

### 2단계 — 통계 대시보드

쌓인 후기를 집계해 보여주는 화면.

- **도넛 차트** — 평점 분포 또는 카테고리 비중을 한눈에
- **워드클라우드** — 후기에서 자주 등장하는 단어를 크기로 표현
- 선행 조건: 후기 데이터가 어느 정도 쌓여 있어야 한다. 후기 10개짜리 워드클라우드는 정보가 아니라 장식이다

### 3단계 — AI 리뷰 요약 · 감성 분석

이 서비스의 차별화 축.

- **리뷰 요약** — 식당에 쌓인 후기를 한 문단으로 압축해 상세 페이지 상단에 노출
- **감성 분석** — 후기 텍스트에서 긍정/부정 신호를 추출해 별점만으로는 안 보이는 경향 파악

대상 사용자와의 접점: 요약이 "맛있다"에 그치면 의미가 없다. **여럿이 갈 만한 곳인지**(시끄러운지, 자리가 넉넉한지, 예약이 되는지)를 후기에서 뽑아내는 방향으로 설계해야 이 서비스의 요약이 된다.

**선행 조건:** 식당당 후기가 일정 수 이상 쌓여야 의미가 생긴다. 후기가 3~4개인 식당의 요약은 원문을 읽는 것보다 나을 게 없으므로, 최소 임계치를 정하고 그 미만은 요약을 노출하지 않는 설계가 필요하다.

**2단계와의 관계:** 워드클라우드는 단어 빈도만 세지만, 감성 분석은 그 단어가 칭찬인지 불만인지까지 판단한다. 워드클라우드를 먼저 만들어두면 3단계로 자연스럽게 확장된다.

---

## 5. 랜딩 페이지 화면 구성

위에서 아래로 5개 섹션.

### 5.1 Hero

**3초 안에 무슨 서비스인지 알게 하는 것**이 이 섹션의 유일한 목표.

- 서비스 이름
- 한 줄 소개 — "약속 장소 정하는 부담"에 말을 거는 문장. 서비스 설명보다 사용자 상황을 먼저 짚는다
- 검색창 또는 시작 버튼 (검색창 결합형 권장 — 서비스 성격이 즉시 전달된다)

### 5.2 인기 맛집 카드

- 맛집 카드 3~6개
- 카드 구성: 썸네일 + 평점 + 식당명 + 지역·분류
- **V1은 가짜 데이터.** 다음 단계에서 실데이터로 교체
- 가짜 데이터라도 실제로 있을 법한 식당명과 지역을 쓴다

### 5.3 최근 리뷰

별점과 리뷰 한 줄이 흐르는 띠. **서비스가 살아있어 보이게 하는 장치.**

- 한 줄 구성: 평점 + 리뷰 한 줄 + 지역
- 가로 무한 루프, 호버 시 정지

### 5.4 서비스 특징

앞으로 생길 기능을 미리 소개하는 구간.

- AI 리뷰 요약 / 감성 분석 / 통계 대시보드 (로드맵 1~3단계와 대응)
- 각 항목에 **COMING SOON 뱃지**를 달아 미구현임을 명시

> 4장 포지셔닝 메모와 연결: AI 기능은 V1에 없지만, "왜 여기서 리뷰를 보나"에 대한 답이기도 하다. 감추는 대신 미구현임을 정직하게 표시하고 노출한다.

### 5.5 푸터

- 만든 사람
- GitHub 링크
- 프로젝트 허브(MY HUB)로 돌아가는 링크

---

## 6. 미결정 사항

착수 전 정리가 필요한 항목.

- [x] **서비스명** — 어디갈래로 확정 (2026-08-24)
- [ ] **식당 데이터 출처** — 사용자 직접 등록 / 공공데이터 / 외부 API
- [ ] **데이터 소스 일원화** — 필터용 정적 JSON과 후기 저장용 DB를 어떻게 합칠지. 구현 순서(JSON 먼저 → DB 이관 / DB 먼저)도 함께 결정
- [ ] **대시보드의 주체** — 서비스 전체 통계 / 식당별 통계 / 내가 쓴 후기 통계. 셋은 만드는 난이도와 쓸모가 전부 다르다
- [ ] **별점 척도** — 5점 단일 / 항목별(맛·가격·분위기 등) 분리
      → 대상이 모임 주최자이므로 항목별 분리 쪽이 설득력이 커졌다. 단 후기 작성 부담은 늘어난다
- [ ] **모임 관련 정보를 다룰지** — 인원 수용, 예약 가능, 소음 수준 등. 후기 작성 폼과 상세 페이지 구조가 여기에 달려 있다
- [ ] **가짜 데이터 준비** — 식당명·지역·평점·리뷰 문구. 인기 맛집 3~6개 + 최근 리뷰 띠 분량
- [ ] **썸네일 이미지 조달** — 인기 맛집 카드용
- [ ] **Hero 검색창의 미동작 처리** — 비활성 / 입력만 가능 / 알림 문구
- [ ] **기술 스택**
